# Daily Log — 2026-08-14

## Outcome

Delivered the first live SentinelSRE reliability milestone.

## Shipped

- Dependency-free checkout reference service with Prometheus metrics.
- Controlled latency and error fault modes limited to the local lab.
- Repeatable traffic generator.
- OpenTelemetry Collector metrics pipeline.
- Prometheus scraping and agent-generated SLO alert loading.
- Grafana provisioning of the agent-generated golden-signals dashboard.
- Docker Compose environment and one-command controls.
- End-to-end live-stack verifier.
- CI job that starts the complete stack and proves telemetry reaches the alerts
  and dashboard.

## Evidence

- 141 unit and behavior tests pass.
- Four containers started successfully and remained healthy.
- Normal traffic produced 59 successful requests.
- Injected error traffic produced 62 observed failures from 78 requests.
- Prometheus received 137 requests through OpenTelemetry.
- Generated SLO alerts entered pending state with healthy rule evaluation.
- Grafana loaded the generated checkout golden-signals dashboard.

## Safety Notes

- Fault injection is explicitly local-demo-only.
- No live cloud credentials or production actions are present.
- Production-style alert waiting periods remain intact rather than being
  shortened solely to make the demo appear faster.

## Milestone 2 — Kubernetes and Helm

- Added a reusable Helm chart for the checkout workload.
- Added startup, readiness, and liveness probes.
- Added non-root execution, RuntimeDefault seccomp, read-only root filesystem,
  dropped capabilities, and disabled service-account token mounting.
- Added requests/limits, HPA, PodDisruptionBudget, NetworkPolicy, topology
  spreading, preferred anti-affinity, and zero-unavailable rolling updates.
- Created a three-node Kind cluster and installed the chart.
- Verified two healthy replicas running on separate worker nodes.
- Verified the hardened runtime settings from live Pod specifications.
- Verified in-cluster service networking with a real request.
- Added manifest and live-cluster jobs to continuous integration.
- Expanded the full test suite to 149 passing tests.

During validation, a strict topology constraint initially deadlocked a rolling
update because a tainted control-plane node was counted as an empty topology
domain. The chart now explicitly honors node taints and affinity when calculating
eligible topology domains. The corrected rollout completed with one replica on
each worker node.

## Milestone 3 — Multi-cloud Terraform foundations

- Added reusable, provider-native modules for private Amazon EKS and Azure AKS.
- Added encrypted audit logging, workload identity, network isolation, managed
  node autoscaling, and multi-zone availability defaults.
- Added cost-conscious development roots for AWS and Azure; neither performs an
  automatic apply and AWS NAT remains explicitly opt-in.
- Added encrypted remote-state templates and documented the credential, plan,
  review, and apply boundary.
- Added an executable validation gate covering format, initialization, provider
  schemas, TFLint, and native Terraform tests.
- Proved three mocked plan cases: EKS private/multi-AZ/encrypted defaults, EKS
  public endpoint guardrails, and AKS private/workload-identity/policy defaults.

## Milestone 4 — GitOps and supply-chain security

- Added an Argo CD project and application with scoped destinations, automated
  pruning, drift self-healing, retry backoff, and orphan warnings.
- Added digest-aware Helm rendering and a guarded image-promotion utility.
- Added Conftest admission rules for immutable images, resource controls, and
  container hardening, executed against the rendered GitOps release.
- Added a fail-closed Kyverno rule for Cosign keyless signatures from the exact
  GitHub Actions workload identity.
- Added Trivy repository/IaC/secret and container gates plus a CycloneDX SBOM.
- Added tag-only GHCR publishing, OIDC signing, and signature identity
  verification. New third-party setup actions are pinned to commit SHAs.
- Locally proved zero high/critical findings in both the repository scan and
  the built checkout container.

## Milestone 5 — Governed SRE agents

- Added a live Incident Investigator Agent that gathers health, checkout, and
  fault-mode evidence without mutation privileges.
- Added typed action requests with risk, evidence, verification, and recovery
  requirements plus deterministic request identifiers.
- Added a policy engine covering agent/action allowlists, environment boundaries,
  exact targets, parameter constraints, and replica blast-radius limits.
- Added seeded dummy identities with role and active-status enforcement.
- Added expiring request-bound HMAC approvals and separation of duties.
- Added fixed adapters for local recovery and narrowly scoped Kubernetes restart
  and scale actions; there is no arbitrary shell adapter.
- Added post-action verification and an append-only hash-chained audit log with
  explicit documentation of its local-storage limitation.
- Ran the live flow: detect injected errors, deny unapproved apply, approve as a
  separate incident commander, recover checkout, verify health, and validate the
  audit chain.
- Expanded continuous reliability checks to cover governance invariants.
- Expanded the full repository suite to 173 passing tests and removed leaked
  HTTP error-response handles from the workload tests.

## Milestone 6 — Failure exercises, recovery, and postmortems

- Added a Kubernetes chaos runner that resolves one exact target and refuses
  non-lab contexts, single-replica deployments, missing PDB protection, or
  same-node replica placement.
- Deleted a live checkout Pod, observed a replacement UID, and measured full
  2/2 recovery in 2.443 seconds; the hardened runtime check passed afterward.
- Added the Pod-loss experiment to the ephemeral Kind CI job so resilience is
  exercised continuously rather than documented theoretically.
- Captured a second live checkout error incident, governed recovery, request
  digest, audit event hash, and verification evidence in the repository.
- Added a deterministic blameless postmortem generator with impact, detection,
  timeline, root cause, contributing factors, improvements, owners, and evidence
  integrity references.
- Added a drift gate that reconstructs the postmortem from evidence, verifies
  the audit chain, and validates the bounded Pod-loss outcome.
- Expanded the full repository suite to 181 passing tests.
